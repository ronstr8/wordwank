server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

# WebSocket proxy removed – gatewayd service disabled

# Player API proxy removed – Perl backend handles /players
}
